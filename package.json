{
  "name": "bloomly-women-wellness-web-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix bloomly-frontend start",
    "build": "npm --prefix bloomly-frontend run build",
    "test": "npm --prefix bloomly-frontend test",
    "install:frontend": "npm --prefix bloomly-frontend install"
  }
}
